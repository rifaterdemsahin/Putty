# Local .terraform directories
**/.terraform/*
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*
Symbols/terraform-aws-ec2/.terraform/providers/registry.terraform.io/hashicorp/aws/5.73.0/linux_amd64/terraform-provider-aws_v5.73.0_x5

# Crash log files
crash.log

# Exclude any .tfvars files that are likely to contain sensitive data, such as passwords, private keys, and other secrets
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and should not be shared
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include .terraformrc and terraform.rc files to prevent local settings from being shared
.terraformrc
terraform.rc